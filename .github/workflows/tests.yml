name: brew test-bot
on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read
  packages: write

jobs:
  test-bot:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-20.04      # Intel Linux (Legacy)
          - ubuntu-22.04      # Intel Linux (Standard)
          - ubuntu-24.04      # Intel Linux (Modern)
          - ubuntu-22.04-arm  # ARM Linux (Standard ARM)
          - ubuntu-24.04-arm  # ARM Linux (Modern ARM)
          - macos-14          # Apple Silicon (Sonoma)
          - macos-15          # Intel/Apple Silicon (Sequoia)
          - macos-latest      # macOS 26 Tahoe
    runs-on: ${{ matrix.os }}
    steps:
      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@master

      - name: Run brew test-bot --only-cleanup-before
        run: brew test-bot --only-cleanup-before

      - name: Run brew test-bot --only-setup
        run: brew test-bot --only-setup

      - name: Run brew test-bot --only-formulae
        run: |
          # This builds the 'Pills' (Bottles) for 8 different architectures!
          brew test-bot --only-formulae --root-url=https://ghcr.io/v2/${{ github.repository_owner }}/cmix cmix

      - name: Upload bottles as artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bottles_${{ matrix.os }}
          path: '*.bottle.*'
